[tool.poetry]
name = "rokoai"
version = "0.1.0"
description = "A Python package for content moderation"
authors = ["Aman", "Harsh"]
license = "MIT"
readme = "README.md"
packages = [
    { include = "moderail" }
]

[tool.poetry.dependencies]
python = "^3.12"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[[tool.poetry.source]]
name = "testpypi"
url = "https://test.pypi.org/legacy/"
