[project]
name = "SigScaffold"
version = "0.1.0"
description = "A tool to derive parameter information from Python functions and classes for contract generation."
authors = [{name = "Jake"}]
readme = "README.md"
requires-python = ">=3.8"
dependencies = []

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
