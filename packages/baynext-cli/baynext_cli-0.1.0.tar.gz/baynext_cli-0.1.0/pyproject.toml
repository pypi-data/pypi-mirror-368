[project]
name = "baynext-cli"
version = "0.1.0"
description = "The Baynext CLI lets you manage your Baynext projects directly from the terminal"
readme = "README.md"
authors = [
    { name = "Léo Grosjean", email = "leo.grosjean@live.fr" }
]
requires-python = ">=3.12"
dependencies = [
    "baynext-py>=0.1.0",
    "python-dotenv>=1.1.1",
    "typer>=0.16.0",
]

[project.scripts]
baynext = "baynext_cli.main:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
