[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "clasherjk"
version = "0.1.0"
description = "A Python wrapper for Clash of Clans API via Jk's proxy"
authors = [
    { name="Jk Mini Dev", email="your-email@example.com" }
]
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.8"
dependencies = ["httpx"]

[project.urls]
Homepage = "https://github.com/JkMiniDev/clasherjk"
Repository = "https://github.com/JkMiniDev/clasherjk"