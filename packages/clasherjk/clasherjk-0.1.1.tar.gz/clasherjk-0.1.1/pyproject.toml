[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "clasherjk"
version = "0.1.1"
description = "Clash of Clans API wrapper using proxy"
readme = "README.md"
license = { text = "MIT" }
authors = [
    { name="Your Name", email="you@example.com" }
]
dependencies = ["requests"]

[tool.setuptools.packages.find]
where = ["."]