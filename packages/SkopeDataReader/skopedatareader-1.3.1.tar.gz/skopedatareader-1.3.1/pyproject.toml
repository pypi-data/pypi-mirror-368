[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name='SkopeDataReader'
version='1.3.1'
description = 'Python reader to load data acquired with skope-fx'
readme = "README.md"
authors = [{name = "Yolanda Duerst", email = "yolanda.duerst@skope.ch"}]
dependencies = ['numpy', 'scipy']