[project]
name = "ragflow-mcp"
version = "1.2.0"
description = "A mcp server that talk with my ragflow"
readme = "README.md"
authors = [{name = "Cheson"}]
requires-python = ">=3.12"
dependencies = ["fastmcp", "requests", "httpx"]

[project.scripts]
ragflow-mcp = "mcp_server_stdio:main"

[tool.hatch.build.targets.wheel]
packages = ["src/"]
