[project]
name = "agnets"
version = "0.1.1"
description = "A multi-agentic python framework."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp>=1.12.0",
    "pydantic>=2.11.7",
]

[project.optional-dependencies]
ollama = [
    "ollama>=0.5.1",
]
openai = [
    "openai>=1.98.0",
]
