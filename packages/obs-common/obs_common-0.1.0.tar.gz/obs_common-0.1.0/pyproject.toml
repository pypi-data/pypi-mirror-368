[project]
name = "obs-common"
version = "0.1.0"
description = "Placeholder for the obs-common package name to avoid dependency confusion."
readme = "README.md"
authors = [
    { name = "Sven Marnach", email = "sven@mozilla.com" }
]
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
