[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ramid-yelp-conn"
version = "0.1.0"
description = "A package to generate fake personally identifiable information"
authors = [
    { name = "Your Name", email = "you@example.com" }
]
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.8"
dependencies = [
    "faker"
]

[tool.setuptools.packages.find]
where = ["src"]
