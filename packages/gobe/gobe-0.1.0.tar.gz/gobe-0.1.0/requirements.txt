# Core dependencies
click>=8.0.0
jinja2>=3.0.0
watchdog>=2.0.0

# Optional dependencies (install with extras)
# For WebSocket support: pip install gobe-framework[websocket]
# For GraphQL support: pip install gobe-framework[graphql] 
# For cache backends: pip install gobe-framework[cache]
# For database backends: pip install gobe-framework[database]
# For deployment: pip install gobe-framework[deployment]
# For optimization: pip install gobe-framework[optimization]
# For all features: pip install gobe-framework[all]
