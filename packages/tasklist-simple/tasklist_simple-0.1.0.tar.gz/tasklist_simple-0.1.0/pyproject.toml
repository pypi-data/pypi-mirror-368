[project]
name = "tasklist-simple"
version = "0.1.0"
description = "Simple task manager via terminal"
authors = [
    {name = "Bahanna-01",email = "bhsantos1424@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.9"
dependencies = [

]

[tool.poetry]
packages = [{include = "tasklist_simple", from = "src"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
tasklist = "tasklist_simple.cli:main"
