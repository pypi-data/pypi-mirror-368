[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "SNFit"
version = "1.1.0"

dependencies = [
    "numpy",
    "dash",
    "plotly",
    "matplotlib",
    "pandas"
]