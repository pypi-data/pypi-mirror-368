[project]
name = "psi_toml"
version = "0.0.13"
description = "A very lite implementation of a toml parser, reader, and writer"
authors = [
    {name = "Jeff",email = "jeffwatkins2000@gmail.com"}
]
readme = "README.md"
requires-python = '==3.13.*'
dependencies = []

[dependency-groups]
dev = ['pytest']

[build-system]
requires = ['uv-build']
build-backend = 'uv_build'
