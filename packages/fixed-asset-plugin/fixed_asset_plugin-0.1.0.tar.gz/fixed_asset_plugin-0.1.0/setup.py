# -*- coding: utf-8 -*-

import importlib
import importlib.util
import os
import setuptools

"""This file is used to package the FixedAssetPlugin plugin.

- It was generated by the InvenTree Plugin Creator tool - version 1.1.0
- Ref: https://github.com/inventree/plugin_creator
"""

"""Read the plugin version from the source code."""
module_path = os.path.join(os.path.dirname(__file__), "fixed_asset_plugin", "__init__.py")
spec = importlib.util.spec_from_file_location("fixed_asset_plugin", module_path)
fixed_asset_plugin = importlib.util.module_from_spec(spec)
spec.loader.exec_module(fixed_asset_plugin)

with open('README.md', encoding='utf-8') as f:
    long_description = f.read()

setuptools.setup(
    name="fixed_asset_plugin",
    version=fixed_asset_plugin.PLUGIN_VERSION,
    author="Adrian Piney Gutierrez",
    author_email="adrian.piney@unican.es",
    description="Add Fixed Asset Parameter to every new Part created",
    long_description=long_description,
    long_description_content_type='text/markdown',
    license="MIT",
    packages=setuptools.find_packages(),
    include_package_data=True,
    install_requires=[
        "inventree>=0.17.0"
    ],
    setup_requires=[
        "wheel",
        "twine",
    ],
    python_requires=">=3.9",
    entry_points={
        "inventree_plugins": [
            # "FixedAssetPlugin = fixed_asset_plugin.core:FixedAssetPlugin"
            "fixed-asset-plugin = fixed_asset_plugin.core:FixedAssetPlugin"
        ]
    },
)
