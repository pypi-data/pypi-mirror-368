# Lazy imports - functions are discovered dynamically by the registry
# This avoids heavy imports during CLI startup

# Note: Individual functions are imported as needed by the function registry
# to avoid loading all dependencies during CLI initialization.
