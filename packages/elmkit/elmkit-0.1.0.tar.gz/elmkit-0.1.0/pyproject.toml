[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "elmkit"
version = "0.1.0"
description = ""
readme = "README.md"
requires-python = ">=3.13"
authors = [{ name = "dontwakup", email= "jfwu.ai@hotmail.com" }]
dependencies = [
    "openai>=1.99.3",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["elmkit*"]