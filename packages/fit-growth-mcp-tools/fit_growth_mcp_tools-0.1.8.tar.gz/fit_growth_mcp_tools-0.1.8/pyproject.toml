[project]
name = "fit-growth-mcp-tools"
version = "0.1.8"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.7"
dependencies = [
    "requests>=2.32.4",
]

[project.scripts]
fit-growth-mcp-tools = "fit_growth_mcp_tools:main"

[build-system]
requires = ["uv_build>=0.8.4,<0.9.0"]
build-backend = "uv_build"
