[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "repviz"
version = "0.1.1"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "scikit-learn>=1.6.1",
    "seaborn>=0.13.2",
    "torch>=2.6.0",
]

[tool.setuptools]
packages = ["repviz"]
