[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "api-logger"
version = "0.1.0"
description = "Logs API requests and responses and uploads them to S3"
authors = [
    { name = "Your Name", email = "you@example.com" }
]
dependencies = [
    "aioboto3",
    "fastapi"
]
