[package]
name    = "{{ name }}"
version = "0.1.0"
edition = "2024"

[lints.rust]
unused_imports = "allow"

[dependencies]
indoc   = { version = "{{ indoc_version }}" }
dmoj    = { version = "{{ dmoj_version }}" }

[dev-dependencies]
assert_cmd  = { version = "{{ assert_cmd_version }}" }
