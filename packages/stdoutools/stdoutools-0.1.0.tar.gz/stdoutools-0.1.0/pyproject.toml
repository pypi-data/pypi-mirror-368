[project]
name = "stdoutools"
version = "0.1.0"
description = "Tools for the python system output, or stdout."
authors = [{name = "Juno", email = "junowu125@gmail.com"}]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.13"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
