[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"


[project]
name = "xl_durel_utils"
version = "1.0.4"
description = "XL-DURel Utility Functions"

readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.8"

dependencies = [
    "pandas",
    "numpy",
    "scipy",
    "tabulate",
    "krippendorff",
    "transformers",
    "matplotlib",
    "seaborn"
]