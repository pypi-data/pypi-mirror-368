[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "xl_durel-utils"
version = "1.0.1"
description = "Utility function for tokenizing, truncating, and decoding sentences with marked spans."

readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.8"