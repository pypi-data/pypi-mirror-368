[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"


[project]
name = "xl_durel-utils"
version = "1.0.2"
description = "XL-DURel Utility functions"

readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.8"

dependencies = [
    "pandas",
    "numpy",
    "scipy",
    "tabulate",
    "krippendorff",
    "transformers",
    "matplotlib"
]

