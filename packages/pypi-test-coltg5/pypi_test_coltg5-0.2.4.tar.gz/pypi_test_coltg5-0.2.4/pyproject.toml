[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"


[project]
name = "pypi-test-coltg5"
version = "0.2.4"
description = "testing building and publishing a uv project w hatchling to pypi"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.3.2",
]

[tool.hatch.build.targets.wheel]
packages = [
    "src/pypi_test_coltg5",
    "src/yeahyeah",
]
