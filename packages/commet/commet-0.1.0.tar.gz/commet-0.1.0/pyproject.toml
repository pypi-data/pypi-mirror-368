[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "commet"
version = "0.1.0"
description = ""
authors = []
readme = ""
requires-python = ">=3.7"
license = { text = "MIT" }  # Adjust if you have a different license

[tool.setuptools.packages.find]
where = ["."]
include = ["*"]

[tool.setuptools]
include-package-data = true

[tool.setuptools.data-files]
"bin" = ["bin/commet_solve"]
"lib" = [
    # "lib/libc10.so",
    # "lib/libdeal_II.so.9.7.0",
    # # "lib/libgmsh.so.4.13",
    # "lib/libgomp-98b21ff3.so.1",
    # "lib/libHYPRE-2.31.0.so",
    # "lib/libmetis.so",
    # "lib/libp4est.so.1",
    # "lib/libparmetis.so",
    # "lib/libpetsc.so.3.22",
    # "lib/libscalapack.so.2.2",
    # "lib/libsc.so.1",
    # # "lib/libtorch_cpu.so",
    # # "lib/libtorch.so"
]
