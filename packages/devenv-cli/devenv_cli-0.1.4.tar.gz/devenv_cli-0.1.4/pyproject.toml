[tool.poetry]
name = "devenv-cli"
version = "0.1.4"
description = ""
authors = ["Shuchir Jain <pip@shuchir.dev>"]
readme = "README.md"
packages = [{include = "devenv_cli", from = "src"}]

[tool.poetry.dependencies]
python = "^3.10"
questionary = "^2.1.0"
docker = "^7.1.0"
typer = "^0.16.0"
yaspin = "^3.1.0"

[tool.poetry.scripts]
devenv = "devenv_cli.main:app"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
