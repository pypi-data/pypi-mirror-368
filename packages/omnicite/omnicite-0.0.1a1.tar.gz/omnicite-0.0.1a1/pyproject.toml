[build-system]
requires = ["setuptools>=45", "wheel", "setuptools_scm[toml]>=6.2"]
build-backend = "setuptools.build_meta"

[project]
name = "omnicite"
version = "0.0.1a1"
description = "Universal citation management and academic reference toolkit"
authors = [
    {name = "OmniCite Team", email = "team@omnicite.org"}
]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.7"
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers", 
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.7",
    "Programming Language :: Python :: 3.8", 
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering",
    "Topic :: Documentation",
    "Topic :: Text Processing",
]
dependencies = [
    "requests>=2.25.0",
    "beautifulsoup4>=4.9.0", 
    "lxml>=4.6.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=6.0",
    "black>=21.0",
    "flake8>=3.8.0",
]

[project.urls]
Homepage = "https://github.com/omnicite/omnicite"
Documentation = "https://omnicite.readthedocs.io"
Repository = "https://github.com/omnicite/omnicite"
"Bug Tracker" = "https://github.com/omnicite/omnicite/issues"

[project.scripts]
omnicite = "omnicite.cli:main"

[tool.setuptools]
packages = ["omnicite"]

[tool.setuptools.package-data]
omnicite = ["*.json", "*.yaml", "*.yml"]

[tool.black]
line-length = 88
target-version = ['py37']
include = '\.pyi?$'

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py", "*_test.py"]
