
uv init
uv venv --python 3.12 (default is .venv)
uv venv <name> --python 3.12

source .venv/bin/activate (activate mac)
.venv\Scripts\activate (activate windows)

uv pip list
uv tree (dependency tree)

uv run hello.py
uv add <package>
uv remove <package>
uv add pytest --dev (dev dependencies)
uv sync (install all dependencies)
time uv syc (time the sync)

uv python install 3.12
uv python list (list all python versions available)

uvx (install separately)
uvx ruff check (uses separate venv, and runs the tool)

uv tool dir (show tool directory)
uv tool install ruff
uv tool uninstall ruff
uv tool update ruff