[project]
name = "exec-mcp-server"
version = "0.2.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
exec-mcp-server = "exec_mcp_server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
