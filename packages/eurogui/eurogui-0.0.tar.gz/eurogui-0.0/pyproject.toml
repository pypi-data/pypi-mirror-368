[tool.poetry]
name = "eurogui"
version = "0.0"
description = "A modern, hardware-accelerated C++ GUI framework (Python bindings coming soon)."
authors = ["Kalom"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"

[tool.poetry.dev-dependencies]
pytest = "^7.0"
black = "^23.3"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
