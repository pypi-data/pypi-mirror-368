[project]
name = "behave-parallel-runners"
version = "0.1.0"
description = ""
authors = [
    {name = "Олеся Локкина"}
]
readme = "README.md"
requires-python = "<4.0,>=3.14"
dependencies = [
    "behave (>=1.3.0,<2.0.0)"
]

[tool.poetry]
packages = [{include = "behave_parallel_runners"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
