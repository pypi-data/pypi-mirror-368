[build-system]
requires = ['setuptools>=42', 'wheel']
build-backend = 'setuptools.build_meta'

[project]
name = "zwergetl-engine"
version = "0.1.11"
maintainers = [{name = "DX Dataworks"}]
description = "ZwergETL Engine"
readme = "README.md"
requires-python = ">=3.6"

