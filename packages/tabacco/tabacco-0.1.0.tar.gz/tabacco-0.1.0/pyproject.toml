[project]
name = "tabacco"
version = "0.1.0"
description = ""
authors = [
    {name = "Cam McMenamie",email = "c.mcmenamie@unsw.edu.au"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
]

[tool.poetry]
packages = [{include = "tabacco", from = "src"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
