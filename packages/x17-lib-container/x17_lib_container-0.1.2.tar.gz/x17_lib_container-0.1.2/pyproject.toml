[project]
name = "x17-lib-container"
version = "0.1.2"
description = ""
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [
  { name = "Xing Xing", email = "x.xing.work@gmail.com" }
]
dependencies = [
  "build",
  "docker",
  "x17-lib-base"
]

[build-system]
requires = [
  "setuptools>=61.0", 
  "wheel",
]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = [
  "x17_container"
]
include-package-data = true
