from kuroboros.schema import BaseCRD, BaseCRDProp, prop

########################
Remember to run 
`kuroboros generate crd`
after modfying this file
########################

class {{ kind }}Status(BaseCRDProp):
    """
    TODO: Define {{ kind }} status properties
    """

class {{ kind }}(BaseCRD):
    status = prop({{ kind }}Status)
    """ 
    TODO: Add {{ kind }} specs here!
    remember to use something like
    
    image_tag = prop(str, default="redis")
    """


