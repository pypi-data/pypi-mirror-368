[build-system]
requires = ["setuptools>=80"]
build-backend = "setuptools.build_meta"

[project]
name = "ccxt-suri-sdk"
version = "0.0.2"
readme = "README.md"
authors = [{ name = "hyperithm", email = "tech-admin@naver.com" }]
license = { text = "MIT" }
