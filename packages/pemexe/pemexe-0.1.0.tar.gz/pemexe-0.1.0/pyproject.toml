[project]
name = "pemexe"
version = "0.1.0"
description = "🚀 Python Execution Manager - A powerful, modern tool for managing, scheduling, and executing Python scripts and projects with comprehensive logging, flexible scheduling, and dependency management. Perfect for DevOps, data science, and automation workflows."
readme = "README.md"
keywords = ["python", "scheduler", "automation", "devops", "task-runner", "cron", "execution", "management", "scripts", "projects", "cli", "background-jobs", "logging", "dependencies"]
authors = [{name = "Arian Omrani"}]
requires-python = ">=3.13"
dependencies = [
    "apscheduler>=3.11.0",
    "sqlalchemy>=2.0.41",
    "typer>=0.16.0",
    "uv>=0.8.2",
]

[dependency-groups]
dev = []

[project.scripts]
pem = "pem.cli:app"

[project.urls]
Homepage = "https://github.com/arian24b/pem"
Issues = "https://github.com/arian24b/pem/issues"

[tool.ruff]
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "W", "I"] # Standard checks + isort

[tool.uv]
package = true
