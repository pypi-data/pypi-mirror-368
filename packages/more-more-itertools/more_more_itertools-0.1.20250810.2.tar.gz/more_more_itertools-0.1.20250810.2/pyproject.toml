[project]
name = "more-more-itertools"
version = "0.1.20250810.2"
description = "Additions to more-itertools: at_least_n() and at_most_n()"
readme = "README.md"
authors = [
    { name = "Dutcho" }
]
requires-python = ">=3.12"
dependencies = [
    "more-itertools",
]

[dependency-groups]
dev = [
    "mypy",
    "uv",
]

[build-system]
requires = ["uv_build"]
build-backend = "uv_build"
