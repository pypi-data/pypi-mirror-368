[project]
name = "test_-enie" 
version = "0.2.1"
description = "CLI tool to auto-generate test cases for your code"
authors = [{ name = "Kushagra Wadhwa", email = "testgenieofficial@gmail.com" }]
license = "MIT"
readme = "README.md"
requires-python = ">=3.7"
dependencies = ["click==8.1.7",
"requests==2.31.0",
"pathlib2==2.3.7" ]

[project.scripts]
test-genie = "cli.main:cli"

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"