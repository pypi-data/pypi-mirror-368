image: registry.2pi-labs.com/containers/python-build-tools:python-3.9

stages:
  - build
  - deploy

package:
  stage: build
  script:
    - python3 -m build
  artifacts:
    paths:
      - dist/

# docs:
#   stage: build
#   script:
#     - pip install -r docs/requirements.txt
#     - pip install .
#     - sphinx-build -b html docs/ build/docs/
#   artifacts:
#     paths:
#       - build/

upload:
  stage: deploy
  dependencies:
    - package
  rules:
    - if: '$CI_COMMIT_TAG =~ /^([0-9]+)\.([0-9]+)\.([0-9]+)$/ && $PYPI_USERNAME && $PYPI_PASSWORD'
  script:
    - python3 -m twine upload -u $PYPI_USERNAME -p $PYPI_PASSWORD dist/*

# pages:
#   stage: deploy
#   dependencies:
#     - docs
#   rules:
#     - if: '$CI_COMMIT_TAG =~ /^([0-9]+)\.([0-9]+)\.([0-9]+)$/'
#   script:
#     - mv build/docs/ public/
#   artifacts:
#     paths:
#     - public

