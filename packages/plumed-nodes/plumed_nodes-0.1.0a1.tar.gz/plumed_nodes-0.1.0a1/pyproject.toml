[project]
name = "plumed-nodes"
version = "0.1.0a1"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "ase>=3.25.0",
    "rdkit2ase>=0.1.11",
    "zntrack>=0.8.9",
]


[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pytest>=8.4.1",
]
