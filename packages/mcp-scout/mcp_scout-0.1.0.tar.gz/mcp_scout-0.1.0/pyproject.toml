[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mcp-scout"
version = "0.1.0"
authors = [
  { name="Xia Hua", email="xia@traceforce.ai" }
]
description = "Short description"
readme = "README.md"
requires-python = ">=3.7"
license = {text = "MIT"}
