[build-system]
requires = ['setuptools>=42']
build-backend = 'setuptools.build_meta'

[project]
name = "zwergetl-components-aws-s3"
version = "0.1.11"
maintainers = [{name = "dxdataworks"}]
description = "ZwergETL components for AWS s3"
readme = "README.md"
requires-python = ">=3.6"
dependencies = [
    "zwergetl-engine >=0.1.11",
    "boto3 >=1.37.23"
]
