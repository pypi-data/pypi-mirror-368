# GCP Configuration
PROJECT_ID=your-gcp-project-id
LOCATION=us-central1
MODEL_NAME=gemini-2.0-flash-exp

# Credentials - Choose ONE of these approaches:
# Option 1: Set path to service account key file
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/service-account-key.json

# Option 2: Or place serviceAccountKey.json in the project root (fallback)
# No environment variable needed, just place file in repo root

# CLI Retry Configuration (optional)
# MAX_RETRIES=5
# BASE_RETRY_DELAY=10
# MAX_RETRY_DELAY=300
# RATE_LIMIT_DELAY=60

# Docker/API Configuration
API_PORT=8000
API_HOST=0.0.0.0

# Development settings
PYTHON_ENV=development

# For Docker Compose - uncomment and adjust as needed
# COMPOSE_PORT_API=8000