[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mininglab"  # Must be unique on PyPI
version = "0.1.0"
authors = [{name="Solomon Acheampong", email="achesolo@achetech.com"}]
description = "Mining Engineering tools for building mining automated workflow apps"
readme = "README.md"
license = {file = "LICENSE"}
requires-python = ">=3.7"
dependencies = []

[project.urls]
"Homepage" = "https://github.com/achetech/mininglab"
