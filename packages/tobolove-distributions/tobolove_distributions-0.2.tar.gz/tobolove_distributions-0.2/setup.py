from setuptools import setup

setup(name="tobolove_distributions", 
        version="0.2",
        description="Gaussian distributions", 
        packages=['tobolove_distributions'],
        zip_safe=False)