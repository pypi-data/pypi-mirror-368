[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages]
find = {}

[project]
name = "Orbeez"
version = "1.1.3"

dependencies = [
 "numpy",
 "matplotlib",
 "scipy",
 "pillow",
 "astroquery",
 "astropy",
 "tqdm"
]

[tool.setuptools]
py-modules = []