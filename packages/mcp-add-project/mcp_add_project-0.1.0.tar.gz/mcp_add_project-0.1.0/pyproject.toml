[project]
name = "mcp-add-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "macxiang", email = "xiangzp2017@163.com" }
]
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.12.4",
]

[project.scripts]
mcp-add-project = "mcp_add_project:main"

[build-system]
requires = ["uv_build>=0.8.8,<0.9.0"]
build-backend = "uv_build"
