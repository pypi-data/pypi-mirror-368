[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "netbox-cvexplorer"
version = "1.0.18a"
description = "NetBox Plugin zur Anzeige und Verwaltung von CVE-Informationen"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [
    { name = "Tino Schiffel", email = "tino.schiffel@billhost.de" }
]
dependencies = [
]

[project.urls]
Homepage = "https://localrespository/netbox-cvexplorer"
Source = "https://localrespository/netbox-cvexplorer"

[tool.setuptools.packages.find]
where = ["."]