[tool.poetry]
name = "rest-tester"
version = "0.0.4"
description = "Test Application für REST APIs (Server and Client)"
authors = ["David Kracht <david.kracht@mail.de>"]
packages = [
    {include = "rest_tester", from = "src" },
]

[tool.poetry.dependencies]
python = ">=3.9,<3.14"
requests = "^2.32.4"
flask = "^3.0.3"
werkzeug = "^3.0.3"
pyyaml = "^6.0.1"
PySide6 = "^6.7.0"
ruamel-yaml = "^0.18.6"
rfc3986 = "^2.0.0"
jinja2 = "^3.1.0"

[tool.poetry.dev-dependencies]
pytest = "^7.4.4"

[tool.poetry.scripts]
rest-tester = "rest_tester.main:main"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
