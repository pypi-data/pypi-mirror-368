[project]
name = "markdown2pdf-python"
version = "0.2.0"
description = "⚡ Python client for markdown2pdf.ai - Convert markdown to PDF with Lightning payments"
authors = [{ name = "Serendipity AI" }]
requires-python = ">=3.7"
readme = "README.md"
license = { text = "MIT" }
dependencies = [
    "dotenv>=0.9.9",
    "httpx>=0.24.1",
    "requests",
]

[project.urls]
Homepage = "https://markdown2pdf.ai"
Documentation = "https://markdown2pdf.ai"
Repository = "https://github.com/Serendipity-AI/markdown2pdf-python"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[dependency-groups]
dev = []

[tool.uv]
package = false

[[tool.uv.index]]
name = "pypi"
url = "https://pypi.org/simple"
