[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "games_of_probability"
version = "2.0.0"
description = "Provides classes that are used as games of chance and things that based on probability, using the `random` module."
readme = "README.md"
requires-python = ">=3.7"

authors = [
  { name = "Ngô Trần Quang Thiện", email = "quangthienngotran@gmail.com" }
]

license = { text = "GPLv3" }

keywords = ["game", "games", "probability", "chance"]