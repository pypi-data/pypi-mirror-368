[project]
name = "telepop_env"
version = "0.1.2"
description = "Tiny zero-dependency environment variable loader with type casting and .env.example generation"
authors = [{ name = "Ivan Goncharov", email = "telepop.iv@gmail.com" }]
license = { text = "MIT" }
readme = "README.md"
requires-python = ">=3.7"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
