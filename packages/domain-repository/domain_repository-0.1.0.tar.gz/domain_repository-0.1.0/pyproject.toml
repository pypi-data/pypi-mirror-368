[tool.poetry]
name = "domain-repository"
version = "0.1.0"
description = "Domain metamodels for operational excellence platform."
authors = ["joneill116 <your-email@example.com>"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = ">=3.8,<4.0"
pydantic = ">=1.10,<3.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
