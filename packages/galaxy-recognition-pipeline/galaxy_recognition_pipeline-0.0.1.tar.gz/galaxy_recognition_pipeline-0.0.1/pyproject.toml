[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "galaxy_recognition_pipeline"
version = "0.0.1"

dependencies = [
 "numpy",
 "pandas",
 "astroquery",
 "astropy",
 "tqdm",
 "matplotlib",
 "tensorflow",
 "pandas",
 "scikit-learn",
]
