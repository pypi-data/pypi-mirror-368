[project]
name = "dbt-switch"
version = "0.1.1"
description = "A simple CLI to switch between dbt Cloud projects and hosts."
readme = "README.md"
requires-python = ">=3.8"
dependencies = []

[project.scripts]
dbt-switch = "dbt_switch.main:main"
