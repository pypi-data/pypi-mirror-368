[project]
name = "fastdbt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [{ name = "Cody", email = "cody@dkdc.dev" }]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
fastdbt = "fastdbt:main"

[build-system]
requires = ["uv_build"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pre-commit>=4.3.0",
    "ruff>=0.12.8",
]

[[tool.uv.index]]
url = "https://pypi.org/simple"
